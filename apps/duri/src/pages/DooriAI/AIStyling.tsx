import { HeightFitFlex, MobileLayout, theme } from '@duri-fe/ui';

const AIStyling = () => {
  return (
    <MobileLayout backgroundColor={theme.palette.Black}>
      <HeightFitFlex justify="flex-between"></HeightFitFlex>
    </MobileLayout>
  );
};

export default AIStyling;
